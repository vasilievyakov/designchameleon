services:
  - type: web
    name: designchameleon
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
    buildFilter:
      paths:
        - src/**
        - package.json

